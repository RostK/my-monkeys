# IDE / editor
.idea/

# Dependencies
node_modules/

# Build output & CI-generated artifacts (see docs/SPEC-marketplace-ui.md §11).
# The site's own build output and generated catalog are ignored by site/.gitignore.
dist/

# SDD telemetry — machine-local. The retro reports under retros/ are committed;
# the raw ledger and its watermark are per-machine and are not.
retros/ledger.jsonl
retros/.ledger-state/

# pr-self-review gate state (stamps the locally reviewed diff hash — per-machine)
.claude/.pr-self-review-state.json

# OS
.DS_Store
Thumbs.db
